{
  "name": "gugetfin-api",
  "version": "1.0.0",
  "private": true,
  "description": "API oficial do GugetFin para dispositivos e integrações externas.",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check index.js && node --check src/api.js && node --check src/domain.js && node --check src/oauth.js"
  },
  "dependencies": {
    "firebase-admin": "14.4.0",
    "firebase-functions": "7.4.0"
  }
}
